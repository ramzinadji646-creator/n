version: '3.8'
services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - SECRET_KEY=dev-secret-key
      - DATABASE_URL=sqlite:///bakery.db
      - JWT_SECRET_KEY=jwt-secret-key
    volumes:
      - .:/app
